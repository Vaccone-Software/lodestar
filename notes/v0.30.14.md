The slide is seen.

A switch in Settings began to slide and was never seen sliding: a
write re-reads the config and rebuilds the pane, and a rebuilt switch
arrived already at rest before the eye caught the first frame. Each
switch now survives the rebuild, kept by its config path, and only its
state is told; the slide a press started keeps running through it.
