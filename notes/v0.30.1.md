The keyboard never waits for a wedged app.

A summon parks the strays, moves the members, and raises the window,
and three of those were accessibility writes made inside the gesture
itself: a park, an un-minimize, a raise. Tiling moves had already
left for a serial lane; these had not, and an app that answered a
second late took the tap down with it, hotkeys dead until the watchdog
revived them. Placements ran 17 ms at the median and 1.2 s at the
worst, and the worst was the one that mattered. Every write now rides
one lane, parks and moves and raises in the order the gesture asked,
and the keyboard is answered before any of them lands. The exit path
waits for the lane to drain before it goes, so a quit still puts
every parked window back.

Quitting is not dying. The marker a run leaves for its successor now
says a graceful shutdown began before the shutdown's unbounded work,
so a logout that cuts the restore short reads as a quit at the next
boot instead of a crash to recover from. A run that never reached its
shutdown is still a crash, and still recovered.

The coach learns from being right. Its quiets were scaled by each
kind's record and nothing else, and after two offers in seventeen
days, both taken, ten findings stood waiting. Each accept in a row now
halves the channel's quiets, to an eighth after three, and shortens
the days a cue-having suggestion holds out for its cue by the same
scale; one no puts the book rate back at once. The evidence gates are
untouched: this is pacing, never permission.
