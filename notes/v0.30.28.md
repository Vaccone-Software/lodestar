A pick is seen where it is made, the anchor rides ahead of the far end,
and the design language is held by a test.

When a capital narrows the chips, the chips now say so: the letters
already typed wear the accent on every chip they can still complete,
and the chips they can no longer reach are gone. The reference is on
the glass, where the eye is when it picks, and never in the pill.

In select, once the start anchor lands, the anchored word rides ahead
of the far end's search between the pill's wings, with a quiet dot
between the word already taken and the letters being typed. The stage
that a sentence used to announce is legible from the word itself.

The pill can be dragged by its glass, the way the coach's chip can,
and comes back where it was left for the rest of the session, kept on
the screen. The sheet that ? opens is built on the pill's own
proportions and voices: its insets, its gaps, headers in the body voice
and never in caps.

And the design language is now guarded the way the accent has been: a
corner radius, a type size or a symbol configuration written into a
surface as a number fails the build with the file named. Every one of
them lives in the theme, and the last stray few were brought home.
