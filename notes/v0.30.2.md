A window that cannot fill the display lands in the middle of it.

System Settings has a fixed width. A palette has a size of its own.
Asked for the whole display, they take what they can, and the
slice's origin pinned them to its top-left corner, every summon,
where a window one third the screen's width looks abandoned. A
member that comes back smaller than its slice on an axis is now
centered on that axis: read a beat after the moves, when the frames
say what each window actually took, and again from the corrective
pass, which catches an app that answered late either way. A summon
that cannot fill the display lands in the middle of it; a
beside-summon that cannot fill its column lands in the middle of the
column. Only smaller. A window larger than its slice stays where it
is, because centering it would push part of it off screen, and the
pass that absorbs an oversize window along the layout's axis already
owns that case.

The digits follow the stack. Index order read a window's left edge,
and a narrow window centered at the top of a vertical stack has an
edge to the right of the wide one beneath it, which would have made
the top window the second. The digits now read a window's center
along the layout's axis, where a slice apart is a slice apart
whatever the width.
