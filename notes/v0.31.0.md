One door to the keys, and everything that spoke joins the voice.

lode ? is the one chord that shows the keys of wherever you are: the
whole system at idle, a lens's keys inside the lens, a bar's inside the
bar, the settings window's over it. A plain question mark opens nothing
any more, so it is only ever a character. The launcher, Ask and the
commands bar carry no legend now, and no guide carries an instruction
line: the keys live in one place, and that place is complete.

The keys are not a sheet beside the thing they belong to. A bar grows to
hold them beneath its rows, in its own glass, and the pill grows around
its row, which stays put as the foot of the card. It is the one motion
in Lodestar: three tenths of a second on the system's own curve, no
overshoot, the glass leading and the keys fading in a beat behind its
edge. Reduce Motion shows the result in place.

Everything that spoke now speaks in the voice. A launch that takes time
stands as the pill in an opening mode, and only after a third of a
second, so a running app's summon never shows it. The meeting is a
sentence: begins in four minutes, begins in 45 seconds, is beginning,
began three minutes ago, with Join and Dismiss on the two chords every
ask answers to. The boot ask for Accessibility is the same card with no
keys, because until the grant lands no key reaches Lodestar. The walk's
eight lessons say what each is for in a sentence and draw the keys they
teach as rows beneath, in a new order that ends on the sheet, with the
clipboard just before it. The chain guide's header is the chain so far
drawn as the keys it is, led by the wind when the chain is a breath, and
a breath's rows are its apps and nothing else. The last text on the
glass that trailed off is gone.
